[package]
name = "gnome-briefcase"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Explicit, local, bidirectional folder synchronization for GNOME"
repository = "https://github.com/rtomasa/gnome-briefcase"

[features]
default = []
gui = ["dep:gtk", "dep:adw", "dep:glib", "dep:gio", "dep:gettext-rs"]

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
filetime = "0.2"
fs2 = "0.4"
hex = "0.4"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
tempfile = "3.12"
thiserror = "2.0"
uuid = { version = "1.11", features = ["v7", "serde"] }
walkdir = "2.5"

gtk = { package = "gtk4", version = "0.9", features = ["v4_10"], optional = true }
adw = { package = "libadwaita", version = "0.7", features = ["v1_5"], optional = true }
glib = { version = "0.20", optional = true }
gio = { version = "0.20", optional = true }
gettext-rs = { version = "0.7", features = ["gettext-system"], optional = true }

[dev-dependencies]
tempfile = "3.12"

[[bin]]
name = "gnome-briefcase"
path = "src/main.rs"

[profile.release]
lto = "thin"
strip = "symbols"

